query getBusinessesByCategory($searchString: String!) {
    fuzzyBusinessByName(searchString: $searchString) {
      businessId
    }
  }

query getBusinesses($searchString: String) {
  businesses(searchString: $searchString) {
    businessId
    name
    address
    categories {
      name
    }
  }
}
